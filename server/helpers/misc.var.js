/**
 * Some random variables
 */
// Slugify options
exports.slugOpt = {
  replacement: "_",
  remove: /[^a-z0-9_$()+\/-\s]/g,
  lower: true
};

// _users database prefix
exports._usersPrefix = "org.couchdb.user";
